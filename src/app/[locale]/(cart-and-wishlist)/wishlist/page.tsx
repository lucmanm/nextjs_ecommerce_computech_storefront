import { Container } from "@/components/Container";
import { WishItems } from "./_components/CartItems";

const Wishlist = () => {
  return (
    <Container title="Wishlist">
      <WishItems/>
    </Container>
  );
};

export default Wishlist;
